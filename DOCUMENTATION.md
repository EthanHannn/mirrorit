# MirrorIt 文档位置

本项目的权威文档不存放在代码仓库中，而是存放在 OneDrive 同步目录的 `docs` 中。

当前设备路径：

`C:\Users\GMDI\OneDrive\文档\note_everything\all_note\HomeProjects\06projects\镜像管理软件\docs`

跨设备时，请从 OneDrive 根目录按以下相对路径定位：

`note_everything/all_note/HomeProjects/06projects/镜像管理软件/docs`

可在本机设置 `MIRRORIT_DOCS_DIR` 环境变量指向该目录，避免代码路径或 OneDrive 本地路径变化造成依赖。任何新增、移动或废弃的项目文档都必须更新 `docs/index.md`。
