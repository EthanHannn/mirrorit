# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

MirrorIt 面向主要在中国境内工作的开发者，尤其是需要在 Windows 桌面环境中管理 npm、pnpm、Yarn、Go、Cargo、Docker、pip、Maven 和 Flutter/Pub 镜像配置的全栈、前端、后端、Rust、多设备及企业开发者。

用户通常在日常开发、网络异常排查、公司内外网切换、换机或重装系统时使用 MirrorIt。他们需要快速理解本机最终生效的镜像配置来自哪里，并在不泄露凭据、不破坏已有配置的前提下完成修改或恢复。

## Product Purpose

MirrorIt 是本地优先的桌面配置工作台，用于发现、解释、切换、备份和恢复常见开发工具的镜像与软件源配置。成功意味着用户无需记忆各工具分散的配置位置，也能看懂当前生效值、冲突来源、修改影响和恢复路径。

## Positioning

MirrorIt 的差异化机制是“来源轨迹 + 可逆变更”：它不仅展示一个 registry 或镜像地址，还解释环境变量、项目配置和用户配置如何共同得出最终值；任何写入都先形成结构化差异预览和本地快照，再由用户确认应用或回滚。

## Operating Context

- 主要运行在 Windows 桌面端，与终端、编辑器、项目目录及本机开发工具并行使用。
- 用户以工具为单位连续完成扫描、理解来源、检查连接、预览变更、确认应用和必要时回滚。
- 公司内网源、公共镜像和官方源可能频繁切换，项目级与用户级配置可能同时存在。
- 无网络时仍需查看本地配置、历史记录和快照；网络检查仅对用户明确选择的地址执行。

## Capabilities and Constraints

- 桌面壳采用 Tauri，界面采用 React、TypeScript、Vite、Tailwind CSS 与 shadcn/ui。
- Windows 是首发与主要体验平台；macOS 和 Linux 保留配置路径与发布适配能力。
- 支持浅色、深色和跟随系统三种主题。
- 前端只调用已声明的 Tauri 命令，不直接读取用户文件或执行任意 shell 命令。
- 写入前必须展示变更预览、说明作用域和影响文件，并由用户显式确认；成功或部分成功的写入必须具备可恢复快照。
- token、密码、私钥和 Docker 登录凭据不得进入普通导出、日志或诊断内容。
- MVP 不运营公共镜像、不绕过网络或企业安全策略，也不替代包管理器和制品库。

## Brand Commitments

- 产品名称为 MirrorIt，当前仍可视为暂定名称。
- 体验应安静、精密、可信，信息密度适合开发者长期操作。
- Apple 的清晰层级、克制材质、精确排版和自然反馈是设计原则；不得复制 Apple 商标、专有图标、受限字体文件或 macOS 窗口控件。
- Windows 原生桌面习惯优先，Apple 式审美不得牺牲 Windows 用户的熟悉性与效率。

## Evidence on Hand

- 权威产品需求：Obsidian vault 中的 `docs/01-产品/需求说明书.md`。
- 当前体验基线：`docs/02-体验设计/设计规范.md` 与 `docs/02-体验设计/桌面工作区重构设计.md`。
- 技术与安全边界：`docs/03-技术架构/技术架构设计.md` 及各适配器设计文档。
- 仓库内已有可运行的 React/Tauri 实现、Rust 适配器、命令层和脱敏验证记录；不存在可用于宣传的客户、商业指标或性能基准，不得虚构。

## Product Principles

1. 先解释来源，再提供操作。
2. 先预览影响，再允许写入。
3. 每次改变都可追溯、可恢复。
4. 本地优先、默认脱敏、离线可读。
5. 桌面效率与清晰度优先于装饰性极简。

## Accessibility & Inclusion

- 全部功能必须可通过键盘访问，并在浅色和深色主题下提供清晰焦点状态。
- 正常文本对比度至少达到 WCAG AA，状态不能仅通过颜色表达。
- 支持 `prefers-reduced-motion`、`prefers-reduced-transparency` 和高对比偏好；减少动效时仍保留可理解的状态反馈。
- 中文为当前主要界面语言；长路径、URL 和配置值必须可复制、可截断且可查看完整内容。
