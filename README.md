# MirrorIt

MirrorIt is a desktop workspace for inspecting and safely managing local development
tool mirror and registry configuration.

## Development

```powershell
npm install
npm run tauri dev
```

## Validation

```powershell
npm run build
npm run tauri build -- --debug
```
